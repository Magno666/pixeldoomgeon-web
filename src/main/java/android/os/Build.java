/* PixelDoomgeon — Build.VERSION. GPL-3.0-or-later */
package android.os;

/** El juego pregunta la version de Android para decidir cosas de la
 *  interfaz. Se declara alta para que tome siempre el camino moderno. */
public class Build {
    public static class VERSION {
        public static final int SDK_INT = 35;
    }
}
